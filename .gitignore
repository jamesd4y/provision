.idea/

# OpenTofu / Terraform
.terraform/
*.tfstate
*.tfstate.*
crash.log
*.tfplan
# Provider lock files ARE committed: they pin exact provider versions so a
# pipeline six months from now resolves the same ones.

# Generated by scripts/render_tfvars.py — never hand-edit, never commit.
generated.auto.tfvars.json
tofu/*/vars/*.tfvars.json

# Rendered secrets / env files (produced at deploy time from Bitwarden SM)
*.env
!*.env.example
.secrets/
build/

# Ansible
*.retry
ansible/.facts_cache/

# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/
